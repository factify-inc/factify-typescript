/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Physical dimensions in millimeters.
 */
export type QcrV1alphaPhysicalDimensions = {
  heightMm?: number | undefined;
  widthMm?: number | undefined;
};

/** @internal */
export const QcrV1alphaPhysicalDimensions$inboundSchema: z.ZodMiniType<
  QcrV1alphaPhysicalDimensions,
  unknown
> = z.pipe(
  z.object({
    height_mm: types.optional(types.number()),
    width_mm: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "height_mm": "heightMm",
      "width_mm": "widthMm",
    });
  }),
);

export function qcrV1alphaPhysicalDimensionsFromJSON(
  jsonString: string,
): SafeParseResult<QcrV1alphaPhysicalDimensions, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => QcrV1alphaPhysicalDimensions$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'QcrV1alphaPhysicalDimensions' from JSON`,
  );
}
