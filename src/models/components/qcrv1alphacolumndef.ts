/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  QcrV1alphaCellType,
  QcrV1alphaCellType$inboundSchema,
} from "./qcrv1alphacelltype.js";
import {
  QcrV1alphaNumberFormatCategory,
  QcrV1alphaNumberFormatCategory$inboundSchema,
} from "./qcrv1alphanumberformatcategory.js";
import { Value, Value$inboundSchema } from "./value.js";

/**
 * ColumnDef defines a single column in a DataTable.
 */
export type QcrV1alphaColumnDef = {
  /**
   * `Struct` represents a structured data value, consisting of fields
   *
   * @remarks
   *  which map to dynamically typed values. In some languages, `Struct`
   *  might be supported by a native representation. For example, in
   *  scripting languages like JS a struct is represented as an
   *  object. The details of that representation are described together
   *  with the proto support for the language.
   *
   *  The JSON representation for `Struct` is JSON object.
   */
  attributes?: { [k: string]: Value | null } | undefined;
  /**
   * header text
   */
  name?: string | undefined;
  numberFormatCategory?: QcrV1alphaNumberFormatCategory | undefined;
  type?: QcrV1alphaCellType | undefined;
};

/** @internal */
export const QcrV1alphaColumnDef$inboundSchema: z.ZodMiniType<
  QcrV1alphaColumnDef,
  unknown
> = z.pipe(
  z.object({
    attributes: types.optional(
      z.record(z.string(), types.nullable(Value$inboundSchema)),
    ),
    name: types.optional(types.string()),
    number_format_category: types.optional(
      QcrV1alphaNumberFormatCategory$inboundSchema,
    ),
    type: types.optional(QcrV1alphaCellType$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "number_format_category": "numberFormatCategory",
    });
  }),
);

export function qcrV1alphaColumnDefFromJSON(
  jsonString: string,
): SafeParseResult<QcrV1alphaColumnDef, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => QcrV1alphaColumnDef$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'QcrV1alphaColumnDef' from JSON`,
  );
}
