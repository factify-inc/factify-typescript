/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * DuplicateDocument represents a document that is a visual duplicate.
 */
export type DuplicateDocument = {
  /**
   * Document ID of the duplicate.
   */
  documentId?: string | undefined;
  /**
   * When the caller last viewed this document.
   */
  lastViewedAt?: Date | undefined;
  /**
   * Document title.
   */
  title?: string | undefined;
  /**
   * Version ID of the duplicate.
   */
  versionId?: string | undefined;
  /**
   * Version title.
   */
  versionTitle?: string | undefined;
};

/** @internal */
export const DuplicateDocument$inboundSchema: z.ZodMiniType<
  DuplicateDocument,
  unknown
> = z.pipe(
  z.object({
    document_id: types.optional(types.string()),
    last_viewed_at: types.optional(types.date()),
    title: types.optional(types.string()),
    version_id: types.optional(types.string()),
    version_title: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "document_id": "documentId",
      "last_viewed_at": "lastViewedAt",
      "version_id": "versionId",
      "version_title": "versionTitle",
    });
  }),
);

export function duplicateDocumentFromJSON(
  jsonString: string,
): SafeParseResult<DuplicateDocument, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DuplicateDocument$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DuplicateDocument' from JSON`,
  );
}
