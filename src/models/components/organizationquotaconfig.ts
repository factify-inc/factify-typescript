/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { QuotaTier, QuotaTier$inboundSchema } from "./quotatier.js";

/**
 * OrganizationQuotaConfig represents the stored quota configuration (not current usage).
 */
export type OrganizationQuotaConfig = {
  /**
   * When the configuration was created.
   */
  createdAt?: Date | undefined;
  /**
   * The effective limit (limit if set, otherwise tier default).
   */
  effectiveLimit?: number | undefined;
  /**
   * Optional limit override. If not set, uses the configured tier default.
   */
  limit?: number | null | undefined;
  /**
   * The organization ID.
   */
  organizationId?: string | undefined;
  /**
   * QuotaTier represents the organization's quota tier.
   */
  tier?: QuotaTier | undefined;
  /**
   * When the configuration was last updated.
   */
  updatedAt?: Date | undefined;
};

/** @internal */
export const OrganizationQuotaConfig$inboundSchema: z.ZodMiniType<
  OrganizationQuotaConfig,
  unknown
> = z.pipe(
  z.object({
    created_at: types.optional(types.date()),
    effective_limit: types.optional(types.number()),
    limit: z.optional(z.nullable(types.number())),
    organization_id: types.optional(types.string()),
    tier: types.optional(QuotaTier$inboundSchema),
    updated_at: types.optional(types.date()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "effective_limit": "effectiveLimit",
      "organization_id": "organizationId",
      "updated_at": "updatedAt",
    });
  }),
);

export function organizationQuotaConfigFromJSON(
  jsonString: string,
): SafeParseResult<OrganizationQuotaConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrganizationQuotaConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrganizationQuotaConfig' from JSON`,
  );
}
