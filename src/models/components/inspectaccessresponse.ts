/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DocumentPermissionSet,
  DocumentPermissionSet$inboundSchema,
} from "./documentpermissionset.js";

/**
 * InspectAccessResponse contains the caller's permissions.
 */
export type InspectAccessResponse = {
  /**
   * Document ID.
   */
  documentId?: string | undefined;
  /**
   * DocumentPermissionSet contains the permissions the authenticated user has on a document.
   */
  permissionSet?: DocumentPermissionSet | undefined;
};

/** @internal */
export const InspectAccessResponse$inboundSchema: z.ZodMiniType<
  InspectAccessResponse,
  unknown
> = z.pipe(
  z.object({
    document_id: types.optional(types.string()),
    permission_set: types.optional(DocumentPermissionSet$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "document_id": "documentId",
      "permission_set": "permissionSet",
    });
  }),
);

export function inspectAccessResponseFromJSON(
  jsonString: string,
): SafeParseResult<InspectAccessResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InspectAccessResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InspectAccessResponse' from JSON`,
  );
}
