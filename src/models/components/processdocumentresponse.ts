/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ProcessingOutcome,
  ProcessingOutcome$inboundSchema,
} from "./processingoutcome.js";

/**
 * ProcessDocumentResponse contains the processing result.
 */
export type ProcessDocumentResponse = {
  /**
   * Document ID being processed.
   */
  documentId?: string | undefined;
  /**
   * ProcessingOutcome describes the result of a processing trigger request.
   */
  status?: ProcessingOutcome | undefined;
  /**
   * Version ID being processed.
   */
  versionId?: string | undefined;
};

/** @internal */
export const ProcessDocumentResponse$inboundSchema: z.ZodMiniType<
  ProcessDocumentResponse,
  unknown
> = z.pipe(
  z.object({
    document_id: types.optional(types.string()),
    status: types.optional(ProcessingOutcome$inboundSchema),
    version_id: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "document_id": "documentId",
      "version_id": "versionId",
    });
  }),
);

export function processDocumentResponseFromJSON(
  jsonString: string,
): SafeParseResult<ProcessDocumentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProcessDocumentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProcessDocumentResponse' from JSON`,
  );
}
