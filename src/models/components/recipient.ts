/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { DocumentRole, DocumentRole$inboundSchema } from "./documentrole.js";
import { RecipientType, RecipientType$inboundSchema } from "./recipienttype.js";

/**
 * Recipient represents an entity with access to a document.
 */
export type Recipient = {
  documentRole: DocumentRole;
  /**
   * Email (only for user recipients).
   */
  email?: string | undefined;
  /**
   * Recipient ID (TypeID: user_xxx, bot_xxx, or org_xxx).
   */
  id: string;
  /**
   * Display name.
   */
  name?: string | undefined;
  type: RecipientType;
};

/** @internal */
export const Recipient$inboundSchema: z.ZodMiniType<Recipient, unknown> = z
  .pipe(
    z.object({
      document_role: DocumentRole$inboundSchema,
      email: types.optional(types.string()),
      id: types.string(),
      name: types.optional(types.string()),
      type: RecipientType$inboundSchema,
    }),
    z.transform((v) => {
      return remap$(v, {
        "document_role": "documentRole",
      });
    }),
  );

export function recipientFromJSON(
  jsonString: string,
): SafeParseResult<Recipient, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Recipient$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Recipient' from JSON`,
  );
}
