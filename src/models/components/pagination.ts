/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Pagination contains cursor-based pagination metadata.
 *
 * @remarks
 *  Follows Google AIP-158 for pagination field naming.
 */
export type Pagination = {
  /**
   * Whether there are more results in the forward direction.
   */
  hasMore?: boolean | undefined;
  /**
   * Token to retrieve the next page of results.
   *
   * @remarks
   *  Empty if there are no more results in the forward direction.
   */
  nextPageToken?: string | undefined;
  /**
   * Token to retrieve the previous page of results.
   *
   * @remarks
   *  Empty if this is the first page.
   */
  prevPageToken?: string | undefined;
};

/** @internal */
export const Pagination$inboundSchema: z.ZodMiniType<Pagination, unknown> = z
  .pipe(
    z.object({
      has_more: types.optional(types.boolean()),
      next_page_token: types.optional(types.string()),
      prev_page_token: types.optional(types.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "has_more": "hasMore",
        "next_page_token": "nextPageToken",
        "prev_page_token": "prevPageToken",
      });
    }),
  );

export function paginationFromJSON(
  jsonString: string,
): SafeParseResult<Pagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Pagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Pagination' from JSON`,
  );
}
