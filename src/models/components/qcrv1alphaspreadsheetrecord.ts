/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  QcrV1alphaNamedRange,
  QcrV1alphaNamedRange$inboundSchema,
} from "./qcrv1alphanamedrange.js";
import {
  QcrV1alphaSheet,
  QcrV1alphaSheet$inboundSchema,
} from "./qcrv1alphasheet.js";
import {
  QcrV1alphaSourceReference,
  QcrV1alphaSourceReference$inboundSchema,
} from "./qcrv1alphasourcereference.js";
import {
  QcrV1alphaSpreadsheetMetadata,
  QcrV1alphaSpreadsheetMetadata$inboundSchema,
} from "./qcrv1alphaspreadsheetmetadata.js";

/**
 * SpreadsheetRecord is the QCR representation of an ingested spreadsheet
 *
 * @remarks
 *  workbook (XLSX, CSV, Parquet). Immutable once extracted. Source-format-agnostic.
 *
 *  Hierarchy: SpreadsheetRecord 1:M Sheet 1:M DataTable/SheetImage/SheetChart.
 *  The semantic core is DataTable: named, typed columns applied to ordered rows.
 *  The extractor identifies headers and schemas so consumers don't have to.
 */
export type QcrV1alphaSpreadsheetRecord = {
  id?: string | undefined;
  metadata?: QcrV1alphaSpreadsheetMetadata | undefined;
  namedRanges?: Array<QcrV1alphaNamedRange> | undefined;
  sheets?: Array<QcrV1alphaSheet> | undefined;
  /**
   * SourceReference links a record back to the source it was extracted from.
   *
   * @remarks
   *
   *  Three typed fields identify WHAT, WHERE, and WHEN. Everything else —
   *  format, content hash, storage location, checksums, external URLs — goes
   *  in the attributes bag, because the relevant metadata varies by source type.
   *
   *  Examples by source type:
   *
   *    File upload:
   *      source_id  = "intg_01j8k..."                    (upload channel instance)
   *      remote_id  = "req_01h2xcejqtf2nbrexx3vqjhp41"   (upload request ID)
   *      attributes = {format: "pdf", location: "uploads/.../original.pdf",
   *                    content_hash: "a1b2c3...", storage_etag: "\"abc\""}
   *
   *    Gmail integration:
   *      source_id  = "intg_01k9m..."                    (Gmail connector instance)
   *      remote_id  = "18abc123def"                       (Gmail message ID)
   *      attributes = {format: "eml", thread_id: "...",
   *                    external_url: "https://mail.google.com/..."}
   *
   *    Confluence sync:
   *      source_id  = "intg_01p3q..."                    (Confluence connector instance)
   *      remote_id  = "12345678"                          (Confluence page ID)
   *      attributes = {format: "html", space_key: "ENG",
   *                    external_url: "https://acme.atlassian.net/wiki/..."}
   */
  source?: QcrV1alphaSourceReference | undefined;
};

/** @internal */
export const QcrV1alphaSpreadsheetRecord$inboundSchema: z.ZodMiniType<
  QcrV1alphaSpreadsheetRecord,
  unknown
> = z.pipe(
  z.object({
    id: types.optional(types.string()),
    metadata: types.optional(QcrV1alphaSpreadsheetMetadata$inboundSchema),
    named_ranges: types.optional(z.array(QcrV1alphaNamedRange$inboundSchema)),
    sheets: types.optional(z.array(QcrV1alphaSheet$inboundSchema)),
    source: types.optional(QcrV1alphaSourceReference$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "named_ranges": "namedRanges",
    });
  }),
);

export function qcrV1alphaSpreadsheetRecordFromJSON(
  jsonString: string,
): SafeParseResult<QcrV1alphaSpreadsheetRecord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => QcrV1alphaSpreadsheetRecord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'QcrV1alphaSpreadsheetRecord' from JSON`,
  );
}
