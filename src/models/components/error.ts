/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Error type for programmatic handling
 */
export const Type = {
  InvalidRequestError: "invalid_request_error",
  AuthenticationError: "authentication_error",
  AuthorizationError: "authorization_error",
  NotFoundError: "not_found_error",
  RateLimitError: "rate_limit_error",
  ApiError: "api_error",
} as const;
/**
 * Error type for programmatic handling
 */
export type Type = OpenEnum<typeof Type>;

export type ErrorT = {
  /**
   * Specific error code for detailed handling
   */
  code: string;
  /**
   * Human-readable error message
   */
  message: string;
  /**
   * Parameter that caused the error (for validation errors)
   */
  param: string | null;
  /**
   * Seconds to wait before retrying (for rate limit errors)
   */
  retryAfter: number | null;
  /**
   * Error type for programmatic handling
   */
  type: Type;
};

/** @internal */
export const Type$inboundSchema: z.ZodMiniType<Type, unknown> = openEnums
  .inboundSchema(Type);

/** @internal */
export const ErrorT$inboundSchema: z.ZodMiniType<ErrorT, unknown> = z.pipe(
  z.object({
    code: types.string(),
    message: types.string(),
    param: types.nullable(types.string()),
    retry_after: types.nullable(types.number()),
    type: Type$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "retry_after": "retryAfter",
    });
  }),
);

export function errorFromJSON(
  jsonString: string,
): SafeParseResult<ErrorT, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ErrorT$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ErrorT' from JSON`,
  );
}
