/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  QcrV1alphaBoundingBox,
  QcrV1alphaBoundingBox$inboundSchema,
} from "./qcrv1alphaboundingbox.js";

/**
 * A single spatial fragment of an element on a specific page.
 */
export type QcrV1alphaLayoutFragment = {
  /**
   * Normalized bounding box relative to the page (0.0–1.0).
   *
   * @remarks
   *  Origin is top-left: (0,0) is the top-left corner of the page,
   *  x increases rightward, y increases downward.
   *  Extraction pipelines must flip the y-axis for bottom-left origin formats (PDF).
   *  To get absolute coordinates, multiply by the page's PhysicalDimensions.
   */
  boundingBox?: QcrV1alphaBoundingBox | undefined;
  /**
   * 0-indexed, references PageGeometry.page_number
   */
  pageNumber?: number | undefined;
  rotationDegrees?: number | undefined;
};

/** @internal */
export const QcrV1alphaLayoutFragment$inboundSchema: z.ZodMiniType<
  QcrV1alphaLayoutFragment,
  unknown
> = z.pipe(
  z.object({
    bounding_box: types.optional(QcrV1alphaBoundingBox$inboundSchema),
    page_number: types.optional(types.number()),
    rotation_degrees: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "bounding_box": "boundingBox",
      "page_number": "pageNumber",
      "rotation_degrees": "rotationDegrees",
    });
  }),
);

export function qcrV1alphaLayoutFragmentFromJSON(
  jsonString: string,
): SafeParseResult<QcrV1alphaLayoutFragment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => QcrV1alphaLayoutFragment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'QcrV1alphaLayoutFragment' from JSON`,
  );
}
