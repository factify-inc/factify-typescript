/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { TimelineEvent, TimelineEvent$inboundSchema } from "./timelineevent.js";

export type GetTimelineResponse = {
  events?: Array<TimelineEvent> | undefined;
  hasMore?: boolean | undefined;
  nextCursor?: string | null | undefined;
};

/** @internal */
export const GetTimelineResponse$inboundSchema: z.ZodMiniType<
  GetTimelineResponse,
  unknown
> = z.pipe(
  z.object({
    events: types.optional(z.array(TimelineEvent$inboundSchema)),
    has_more: types.optional(types.boolean()),
    next_cursor: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "has_more": "hasMore",
      "next_cursor": "nextCursor",
    });
  }),
);

export function getTimelineResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetTimelineResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetTimelineResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetTimelineResponse' from JSON`,
  );
}
